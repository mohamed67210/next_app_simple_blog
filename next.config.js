/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com'], // Ajoutez ici le domaine de votre image
  },
}

module.exports = nextConfig
